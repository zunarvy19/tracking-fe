version: "3.8"

services:
  tflow-web:
    build: .
    container_name: tflow_fe
    restart: unless-stopped
    expose:
      - "9000"
    networks:
      - shared_pg_network

networks:
  shared_pg_network:
    external: true
